export default class RadioOption {
    constructor( name, unCheckedIcon, checkedIcon){
        this.name = name;
        this.unCheckedIcon = unCheckedIcon;
        this.checkedIcon = checkedIcon;
    }
}